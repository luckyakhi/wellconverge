# AGENTS.md

WellConverge follows a single working agreement for all AI coding agents. To avoid drift, the guidance
lives in one place:

👉 **See [`CLAUDE.md`](./CLAUDE.md)** for architecture rules, the spec-driven workflow, conventions, and
how to build/run (note: this machine has no JDK — the backend builds/tests via Docker).

Deeper context lives in [`docs/`](./docs) — start with `docs/architecture.md` and the relevant
`docs/contexts/<context>/spec.md`.
