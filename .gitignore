# Java / Gradle
.gradle/
**/build/
!gradle/wrapper/gradle-wrapper.jar
*.class
*.log

# Node / React
node_modules/
frontend/dist/
frontend/.vite/

# IDE
.idea/
*.iml
.vscode/
.settings/
.project
.classpath

# OS
.DS_Store

# Env / secrets
.env
*.local

# Claude local (personal) settings — share .claude/settings.json only
.claude/settings.local.json
.claude/*.tmp.*

# Terraform — infra/ is the only place AWS resources are declared (ADR-0005).
# .terraform.lock.hcl IS committed: provider versions must be reproducible.
**/.terraform/
*.tfstate
*.tfstate.*
tfplan
crash.log
override.tf
override.tf.json
*_override.tf
*_override.tf.json
# Generated by `terraform output -json`; regenerate rather than commit.
infra/terraform-outputs.json

# Python tools
tools/.venv/
tools/out/
__pycache__/
*.py[cod]
.pytest_cache/
*.egg-info/
.ipynb_checkpoints/
